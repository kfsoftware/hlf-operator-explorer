query GetCA($input: NameAndNamespace!) {
  ca(input: $input) {
    name
    namespace
    yaml
    storage {
      ca {
        used
        usedGB
        free
        freeGB
        size
        sizeGB
        percentageUsed
      }
    }
  }
}
