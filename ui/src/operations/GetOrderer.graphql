query GetOrderer($input: NameAndNamespace!) {
  orderer(input: $input) {
    name
    namespace
    yaml
    storage {
      orderer {
        used
        usedGB
        free
        freeGB
        size
        sizeGB
        percentageUsed
      }
    }
  }
}
